#!/bin/sh

i3run --title $1 -- \
    $TERMINAL --class 'scratchpad' -t $1 \
    -o window.dimensions.columns=130 -o window.dimensions.lines=35 \
    -o window.opacity=0.90 \
    -e "tmux new -A -s $2 \; \
    send-keys \"$3\" C-m"
