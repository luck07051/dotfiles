#!/bin/sh

if [ -z "$1" ]; then
  while true; do
    xsetroot -name "$(dwmbar_content | tr '\n' ' ')"
    sleep 30s
  done
elif [ "$1" == 'refresh' ]; then
  kill "$(pstree -lp | grep -e "dwmbar.*sleep" | sed 's/.*sleep(\([0-9]\+\)).*/\1/')"
else
  >&2 echo "invalid option"
fi
