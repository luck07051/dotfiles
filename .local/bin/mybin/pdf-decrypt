#!/bin/sh

[ -z "$1" ] && { echo "${0##*/} password /path/to/pdf" >&2; exit 2; }

if [ -z "$3" ] || [ "$2" = "$3" ]; then
	opt='--replace-input'
	qpdf "$opt" -password="$1" -decrypt "$2"
else
	qpdf "$opt" -password="$1" -decrypt "$2" "$3"
fi
