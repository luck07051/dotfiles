#!/bin/sh

selected="$(dmenu -i -F -p "ICON" -l 20 < ~/.local/share/icons/nerd_icons | cut -d' ' -f1)"

[ -z "$selected" ] && exit

if [ "$1" = "-a" ]; then
  xdotool type "$selected"
else
  printf "%s" "$selected" | xclip -selection clipboard
  notify-send "'$selected' copied to clipboard." &
fi
