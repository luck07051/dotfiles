#!/bin/sh

printf "$XDG_CONFIG_HOME/X11/xresources\n$XDG_CONFIG_HOME/X11/color" | entr -np sync-xrdb &

printf "$HOME/bm/bookmark" | entr -np sync-bookmark &

printf "$HOME/pkg/$(cat /etc/hostname)" | entr -np package-manager "$HOME/pkg/$(cat /etc/hostname)" &

wait
