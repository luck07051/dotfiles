#!/bin/sh

case $BLOCK_BUTTON in
	1) vl mute ;;
	3) vl switch ;;
	4) vl up ;;
	5) vl down ;;
	6) "$TERMINAL" -e "$EDITOR" "$0" ;;
esac

vol="$(vl)"

[ "$vol" = "muted" ] && printf " " && exit

#   
icon=""
printf "$icon $vol%%"
