#!/bin/sh

case $BLOCK_BUTTON in
  1) notify-send "*__________________*" \
        "$(cal | sed "s/\s$(date '+%e')\s\?/[$(date '+%e')]/")" ;;
	6) "$TERMINAL" -e "$EDITOR" "$0" ;;
esac

date '+%a %b %d  %H:%M '
