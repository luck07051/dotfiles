#!/bin/sh

if [ -x "$(which paru)" ]; then
	paru
elif [ -x "$(which yay)" ]; then
	yay
else
	pacman -Syu
fi

printf "\\nUpgrade complete.\\nPress <Enter> to exit window.\\n\\n"
read -r _

sb-update sb-pacman
