#!/bin/sh

case $BLOCK_BUTTON in
	1) notify-send 'but 1' ;;
	2) notify-send 'but 2' ;;
	3) notify-send 'but 3' ;;
	4) notify-send 'but 4' ;;
	5) notify-send 'but 5' ;;
esac

printf "ﰧ %2.0f%%" $(free | awk '/^Mem:/ {print ($3/$2)*100}')
