#!/bin/sh

case $BLOCK_BUTTON in
	6) "$TERMINAL" -e "$EDITOR" "$0" ;;
esac

printf "%s  %s" "" \
  "$(uptime --pretty |\
      sed -e 's/up //; s/\ years\?,/y/; s/\ weeks\?,/w/' \
          -e 's/\ days\?,/d/; s/\ hours\?,\?.*/h/; s/\ minutes\?/m/')"
